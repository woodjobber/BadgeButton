// swift-tools-version:5.3

import PackageDescription

let package = Package(
    name: "BadgeButton",
    products: [
        .library(
            name: "BadgeButton",
            targets: ["BadgeButton"]),
    ],

    targets: [
        .target(
            name: "BadgeButton",
            dependencies: []),
    ]
)
